package one.digitalinnovation.collections

fun main() {
    //não precisa pré-definir a posição dos itens
    val values = intArrayOf(2, 5, 8, 9, 10, 6)

    values.forEach {
        println(it)
    }
}